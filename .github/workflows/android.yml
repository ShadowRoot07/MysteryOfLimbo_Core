name: Android Build (Mystery of Limbo)

on:
  push:
    branches: [ ShadowRoot-CreateAPK ]
  pull_request:
    branches: [ ShadowRoot-CreateAPK ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar Entorno
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Instalar NDK y Herramientas
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake ninja-build

      - name: Compilar C++ a Binario Android
        run: |
          mkdir build_android
          cd build_android
          cmake .. \
            -DCMAKE_TOOLCHAIN_FILE=$ANDROID_NDK_LATEST_HOME/build/cmake/android.toolchain.cmake \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-26 \
            -DCMAKE_BUILD_TYPE=Release \
            -GNinja
          ninja main

      - name: Generar APK Profesional con AAPT2
        run: |
          # 1. Rutas de herramientas
          export TOOL_PATH=$ANDROID_HOME/build-tools/34.0.0
          export PLATFORM=$ANDROID_HOME/platforms/android-34/android.jar
          
          # 2. Crear un APK base con el Manifiesto BINARIO
          # Esto genera un APK que Android sí puede leer
          $TOOL_PATH/aapt2 link -o base.apk \
            --manifest android/app/src/main/AndroidManifest.xml \
            -I $PLATFORM \
            --version-code 1 --version-name "1.0"
          
          # 3. Preparar las librerías nativas en la ruta correcta
          mkdir -p lib/arm64-v8a
          find build_android -name "libmain.so" -exec cp {} lib/arm64-v8a/ \;
          
          # 4. Agregar la librería al APK (sin compresión para que sea cargable)
          zip -0 base.apk lib/arm64-v8a/libmain.so
          
          # 5. ALINEAR EL APK (Crítico para que no diga "Paquete Inválido")
          $TOOL_PATH/zipalign -v 4 base.apk bin/MysteryOfLimbo_Debug.apk
          
          echo "APK Generado y Alineado correctamente." > bin/build_info.txt

      - name: Commit y Push del APK al Repo
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "ShadowRoot07 (via GitHub Actions)"
          git config --global user.email "shadowroot07@users.noreply.github.com"
          git add bin/
          git commit -m "chore: build android binary [skip ci]" || echo "No hay cambios para subir"
          # Usamos el token directamente en la URL para evitar el error 403
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git ShadowRoot-CreateAPK

